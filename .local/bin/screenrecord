#! /bin/sh

ffmpeg -y \
	-f x11grab \
	-framerate 60 \
	-s "$(xdpyinfo | awk '/dimensions/ {print $2;}')" \
	-i :0.0 \
	-f alsa -i default \
	-r 30 \
	-c:v h264 -crf 0 -preset ultrafast -c:a aac \
	"$HOME/Videos/screenrecord-$(date '+%H%M-%S').mkv" &
notify-send "🎥 screen is recording"
